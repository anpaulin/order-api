play.http.errorHandler = "controllers.ErrorHandler"

# Secret key used for cryptographic operations (signing session cookies, CSRF tokens, etc.)
play.http.secret.key = "changeme-askuity-sample-order-service-secret-key-at-least-32-chars-long"
play.http.secret.key = ${?APPLICATION_SECRET}
play.http.secret.key = ${?PLAY_HTTP_SECRET_KEY}


app {
  event-store {
    # Supported values: "file", "memory", "kafka", "mysql"
    type = "file"
    type = ${?EVENT_STORE_TYPE}

    file-path = "/Users/cnh/IdeaProjects/orders_api/data/audit.log"
    file-path = ${?EVENT_STORE_FILE_PATH}

    kafka {
      bootstrap-servers = "localhost:9092"
      bootstrap-servers = ${?KAFKA_BOOTSTRAP_SERVERS}
      topic = "order-events"
      topic = ${?KAFKA_EVENT_TOPIC}
    }

    mysql {
      url = "jdbc:mysql://localhost:3306/order_db"
      url = ${?MYSQL_EVENT_STORE_URL}
      table = "event_store"
      table = ${?MYSQL_EVENT_STORE_TABLE}
    }
  }

  startup {
    replay-events = true
    replay-events = ${?REPLAY_EVENTS_ON_STARTUP}
  }

  blocking-io-dispatcher {
    executor = "thread-pool-executor"
    throughput = 1
    thread-pool-executor {
      fixed-pool-size = 16
    }
  }
}
